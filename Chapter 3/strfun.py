str1 = "sushree"

print(len(str1))    #return the length

print(str1.endswith("ree"))    #Checks if the string ends with a specified suffix.

print(str1.count("e"))       #print howmany times that letter repeated

print(str1.startswith("su"))      #Checks if the string starts with a specified prefix.

str2 = "Mystic Persona"

print(str2.find("Persona"))       #Searches for a substring in the string and returns the index of the first occurrence. Returns `-1` if not found.

print(str2.replace("persona","person"))    #Replaces a specified phrase with another specified phrase in the string.

("sangeeta").lower()     #Converts all characters in the string to lowercase.

("sangeeta").capitalize      #Capitalizes the first letter of the string.

"sangeeta".upper()      #Converts all characters in the string to uppercase.

"Mystic Persona".strip()     #Removes leading and trailing whitespace from the string.
   
"Mystic Persona".split()      #Splits the string into a list where each word is a list item, based on a specified separator.
   
"-".join("Mystic Persona")      # Joins the elements of an iterable (like a list) into a single string with a specified separator.
   


